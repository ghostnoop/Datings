<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.date.datingfinder">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".WebActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|keyboard"
            android:theme="@style/HiddenTitleTheme"
            android:windowSoftInputMode="adjustResize"/>
        <activity
            android:name=".navigation.MainActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".ChatActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".navigation.SixthActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".navigation.FifthActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".navigation.FourthActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".navigation.ThirdActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".navigation.SecondActivity"
            android:theme="@style/HiddenTitleTheme" />
        <activity
            android:name=".FinderActivity"
            android:theme="@style/HiddenTitleTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

</manifest>